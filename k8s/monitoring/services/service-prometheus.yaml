apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: monitoring-ns
  labels:
    app: prometheus-app
    version: v1
spec:
  selector:
    app: prometheus-app
  ports:
    - port: 9090
      targetPort: 9090
      name: http
  type: ClusterIP

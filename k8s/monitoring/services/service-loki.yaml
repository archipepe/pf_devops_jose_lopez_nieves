apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: monitoring-ns
  labels:
    app: loki-app
    version: v1
spec:
  selector:
    app: loki-app
  ports:
    - port: 3100
      targetPort: 3100
      name: http
  type: ClusterIP

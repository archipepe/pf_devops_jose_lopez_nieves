apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: symfony-ns
  labels:
    app: symfony-app
    version: v1
spec:
  selector:
    app: symfony-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP

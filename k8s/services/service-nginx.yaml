apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: symfony-ns
spec:
  selector:
    app: symfony-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
